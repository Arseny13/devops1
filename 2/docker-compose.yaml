version: '3.8'


services:
  db:
    image: postgres:14
    volumes:
      - /var/lib/postgresql/data/
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    env_file:
      - ./.env
    ports:
      - "5433:5432"